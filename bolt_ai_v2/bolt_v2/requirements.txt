# Bolt AI v2.1 — All free and open source

# ── Core AI ──────────────────────────────────────────────
anthropic>=0.49.0           # Claude API ($5 free credit on signup)
aiohttp>=3.9.0              # Async HTTP for concurrent RSS fetching
feedparser>=6.0.11          # RSS/Atom parsing
requests>=2.31.0            # Sync HTTP
schedule>=1.2.0             # Cron-style scheduler

# ── Voice (all free) — see local_tts.py for voice options ─
edge-tts>=6.1.9             # Microsoft Edge TTS — unlimited free, Azure quality, no key

# ── Video assembly ────────────────────────────────────────
Pillow>=10.0.0              # Thumbnail generation
# FFmpeg via system: sudo apt install ffmpeg / brew install ffmpeg

# ── NLP ──────────────────────────────────────────────────
nltk>=3.8.1                 # Tokenization + sentiment
scikit-learn>=1.3.0         # TF-IDF topic clustering
numpy>=1.24.0               # Numerical ops

# ── Enhanced features ─────────────────────────────────────
python-dateutil>=2.8.2      # Date parsing (cost tracker, backup system)
beautifulsoup4>=4.12.0      # HTML cleaning for RSS content
lxml>=4.9.0                 # Fast HTML/XML parser
python-json-logger>=2.0.0   # Structured JSON logging

# ── Utils ────────────────────────────────────────────────
requests-toolbelt>=1.0.0    # Multipart uploads (YouTube API)
# ── New in v2.2 ───────────────────────────────────────────────
python-dotenv>=1.0.0        # .env file loading (secrets_manager.py)
# sqlite3 is built into Python — no install needed
# ── API server (v2.2) ─────────────────────────────────────────────
fastapi>=0.110.0            # Web framework for the backend API
uvicorn[standard]>=0.27.0   # ASGI server for FastAPI
python-multipart>=0.0.9     # File upload support

# ── SSE / streaming ───────────────────────────────────────────
sse-starlette>=1.8.2        # Server-Sent Events for FastAPI real-time stream
